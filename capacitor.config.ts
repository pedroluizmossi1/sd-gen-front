import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sd.gen',
  appName: "'sd-gen'",
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
